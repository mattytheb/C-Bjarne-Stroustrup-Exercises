#include "std_lib_fac.h"


int main()   

{
	vector<double>ins;
		double in = 0;

	char n = 0;
	cout << "Please enter a double values.\n";
	while (cin >> in)
		ins.push_back(in);
	for (double i = 0; i < in;
	sort(ins.begin(), ins.end()));
	cout << "the smallest number is: " << ins.begin << "the largest number is: " << ins.end << endl;
	
		if (n== '|')
			cout << "terminate";

	
	return 0;
}
